{{#each podcasts as |podcast|}}

<div class="row align-center mb-5">
  <div class="col">
    <h3>{{podcast.title}}</h3>
    <p>{{podcast.description}}</p>
  </div>
</div>
{{else}}
<h6>There are currently no podcast</h6>
{{/each}}